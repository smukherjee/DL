ニューラルネットワークの計算のされ方は
こう構成されていると聞きましたね ニューラルネットワークの出力へ向けての計算は 前方パスか順誤差伝播法の先行する段階と 勾配や微分を計算するような 後方パスか誤差逆伝播法の後続する段階とで構成されます 計算グラフによって
なぜこのように構成されているのかが説明できます このビデオでは
例を見ていきます 計算グラフを説明するために ロジステック回帰や完璧なニューラルネットワークよりも
簡単な例を使いましょう 関数J を計算しようとしているとしましょう ３つの引数a、b、c を
持つ関数です 仮に関数は3(a+bc)としましょう これを計算するには
３つの独自の段階があります まず最初にbc の値が
何かを計算し これを変数u に
保持するとします u = b * cです V=a+uです
最終的には 出力Jは3Vです 計算しようとしている最終的な関数Jは
こうなります この３段階を計算グラフでは
次のように描けます ３つの変数a、b、c をここに書きます 最初に計算するのはu=bc でした これを四角の中に入れます これへの入力はb とc です V=a+u があるでしょう これへの入力は 計算したu と
一緒にa を入れます 最終的にJ=3V が来ます 具体的な例にするとa=5 b=3、c=2 とします
u=bc は６です a+u は11になり
３倍にしてJ=33になります 実際に検証できると思いますが この式は3(5+3x2)のようになり 展開すると J の値として
33が実際に得られます 独特な特別な出力値を
最適化したい場合には この場合はJですが 計算グラフは 便利なものになります 例えばロジステック回帰の場合には J は最小化しようとするコスト関数です この少しの例で分かるのは 左から右へのパスを通して J の値を計算できます このあとの数スライドでは
微分を計算するために 右から左へのこんなパスがあり 青の矢印に対して
逆の方向に進んでいます これは微分を計算するのに
最も自然な方法です まとめますと計算グラフは 計算を左から右の
青色の矢印の方向に計算します 次のビデオでは逆方向の赤矢印の 右から左への微分計算を
どう行うのかを見ていきます 次のビデオに進みましょう