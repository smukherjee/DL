Me has escuchado decir que los cálculos de una red neuronal se organizan en términos de paso hacia adelante o propagación hacia adelante, en el que calculamos la salida de la red neuronal, seguido de un paso hacia atrás o propagación hacia atrás, que utilizamos para calcular gradientes o calcular derivadas. El grafo de computación explica por qué se organiza de esta manera. En este video, daremos un ejemplo. Para ilustrar El grafo de computación, vamos a usar un ejemplo más simple de regresión logística o una red neuronal completa. Digamos que estamos <br />tratando de calcular una función J que es una función de tres variables a, b, y c y digamos que la función es 3 (a + bc). Computando esta función en realidad tiene tres pasos distintos. La primera es que necesitas calcular que es bc y digamos que almacenamos eso en la variable llamada u. Así u = bc y luego puedes calcular V = a * u. Así que digamos que esto es V. Y, finalmente, su salida J es 3V. Así que al final esta es tu función J que estás tratando de calcular. Podemos dar estos tres pasos. y dibujarlos en un grafo de computación
 de la siguiente manera. Digamos, dibujo sus tres variables. a, b, y c aquí. Así que lo primero que hicimos. fue computar u = bc. Así que voy a poner una caja rectangular alrededor de esto. Y así, las entradas son b y c. luego, podría tener V = a + u. Así que las entradas para esto son u computado junto con a. Y, finalmente, tenemos J = 3V Entonces, como ejemplo concreto, si a = 5, b = 3 y c = 2 entonces u = bc sería seis porque a + u sería 5 + 6 es 11 , J es tres veces eso, entonces J = 33. Y de hecho, espero que puedas verificar que esto es tres veces cinco más tres veces dos. Y si expandes esto, en realidad obtienes 33 como el valor de J. Por lo tanto, el grafo de computación es útil cuando hay algunos distinguidos o alguna variable de salida especial, como J, en este caso, que quieres optimizar. Y en el caso de una regresión logística, J es, por supuesto, la función de costo. que estamos tratando de minimizar. Y lo que estamos viendo en este pequeño ejemplo es que, a través de un paso de izquierda a derecha, puedes calcular el valor de J y lo que veremos en el siguiente par de diapositivas es que para calcular las derivadas. habrá un paso de derecha a izquierda, así, como ir en la dirección opuesta de las flechas azules. Eso sería lo más natural para el cálculo de las derivadas. Para recapitular, el grafo de computación organiza un cálculo con estas flechas azules. calculando de izquierda a derecha. Vamos a referirnos al siguiente video. ¿Cómo se puede hacer, como las flecha roja hacia atrás, el cálculo de derecha a izquierda de las derivadas?. Continuemos con el siguiente vídeo.